// Mode 3: Simulated Binaural Ear Cleaning — placeholder
//
// Planned: ASMR-style spatial audio simulating ear cleaning tools (probes,
// brushes, air puffs) moving closely around the listener's head via HRTF
// PannerNodes with precisely scripted position automation.
//
// All exports are no-ops until implementation begins.

export function build(_ctx, _state) {}
export function teardown() {}
export function applyState(_state, _ctx) {}
export function startTimer(_state) {}
export function stopTimer() {}
